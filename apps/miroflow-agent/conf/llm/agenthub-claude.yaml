# conf/llm/agenthub-claude.yaml - AgentHub Claude configuration
defaults:
  - default
  - _self_

provider: "agenthub"
model_name: "claude-sonnet-4-5-20250514"
client_type: "claude-4-5"  # Optional: force specific client type
api_key: ${oc.env:ANTHROPIC_API_KEY}
base_url: null  # Use default
max_context_length: 200000
max_tokens: 16384
temperature: 1.0  # Claude thinking mode requires temperature=1.0
thinking_level: "medium"  # none, low, medium, high
prompt_caching: "enable"  # enable, disable, enhance
